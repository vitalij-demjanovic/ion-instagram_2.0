import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-instagram_2.0',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
